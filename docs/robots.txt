# Allow all bots by default since getting blocked is kinda annoying
User-agent: *
Allow: /

# Block PerplexityBot (don't worry, they'll still fetch it anyway)
User-agent: PerplexityBot
Disallow: /

Sitemap: https://richard-weiss.github.io/sitemap.xml